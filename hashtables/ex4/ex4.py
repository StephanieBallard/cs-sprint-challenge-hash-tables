
number_dictionary = {}

def has_negatives(a):
    """
    YOUR CODE HERE
    """
    # Your code here

    absolute_values = []
    
    for i in a:
        if i is not number_dictionary:
            number_dictionary[i]
        
    
            

    return result


if __name__ == "__main__":
    print(has_negatives([-1, -2, 1, 2, 3, 4, -4]))

# iterate through the input checking if the opposite or its negative is already in the dictionary

# if it is you can add the abs(num) to the output array

# if it isn't you still want to add it to the dictionary in case the opposite number as in positive num or negative num comes about



